# Smoke test autenticado — Atleta Hib v4.1.1

Data: 2026-07-10  
Ambiente: frontend Vite local + Supabase configurado

## Resultado

| Cenário | Resultado | Evidência observada |
|---|---|---|
| Migration — colunas de refeição | PASS | REST reconheceu `source`, `import_method`, `confidence` e `dedupe_key` com HTTP 200. |
| Migration — check-in dividido | PASS | REST reconheceu campos de manhã/noite com HTTP 200. |
| Migration — água atômica | PASS | RPC existe e recusou chamada anônima com `401 / 42501`, confirmando proteção por usuário. |
| Água — cliques consecutivos | PASS | Dois incrementos de 500 ml foram preservados; valor foi restaurado ao original. |
| Comida manual sem macros | PASS | Kcal foi registrada e macros apareceram como valores conhecidos/total parcial, não como zero confirmado. |
| Comida JSON — origem | PASS | Registro importado apareceu identificado como JSON. |
| Comida JSON — dedupe | PASS | Segunda importação idêntica foi bloqueada com mensagem explícita de duplicidade. |
| Check-in manhã/noite | PASS | As duas notas temporárias permaneceram independentes ao alternar entre os modos. |
| Cardio 20:08 | PASS | Histórico exibiu duração `20:08`, sem arredondar para 20 minutos. |
| Cardio sem distância | PASS | Histórico exibiu `sem distância`, não `0.00 km`. |
| Kcal de sessão | PASS | Cardio exibiu kcal como detalhe e badge `não soma`. |
| Relatório semanal | PASS | Tela carregou sem erro, manteve ausência de peso como `-- / sem tendência` e cardio voltou à contagem original após limpeza. |
| Limpeza | PASS | Refeições, notas e cardio temporários foram removidos; água e totais do dia voltaram aos valores anteriores. |

## Validação automatizada

- `npm run test`: 19 testes passaram.
- `npm run lint`: passou.
- `npm run typecheck`: passou.
- `scripts/audit-v411.ps1 -SkipBuild`: passou.

## Limite do smoke test

O rollback compensatório de uma sessão mista força + cardio não foi provocado no ambiente real, pois isso exigiria criar uma segunda sessão de força indistinguível do treino real já existente e forçar artificialmente uma falha de banco/rede. O caminho normal de cardio foi validado; o rollback permanece coberto por revisão do fluxo e deve receber um teste de integração isolado com banco de teste em uma etapa futura.
